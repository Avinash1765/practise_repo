package com.example.demo.codeforces;

import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;
import java.util.List;
import java.util.Scanner;

public class SortedAdjacentDiff {

	/*
	 * public static void main(String[] args) { Scanner s=new Scanner(System.in);
	 * 
	 * int t= s.nextInt();
	 * 
	 * while(t-->0) { int n= s.nextInt();
	 * 
	 * List<Integer> list= new ArrayList<>();
	 * 
	 * for(int i=0;i<n;i++) list.add(s.nextInt());
	 * 
	 * Collections.sort(list, new Comparator<T>() { }); } }
	 * 
	 * static class MyComparator implements Comparator<Integer> {
	 * 
	 * @Override public int compare(Integer a, Integer b) { return Math.abs(a-b); }
	 * 
	 * 
	 * 
	 * }
	 */
	
	/*
	 * sort the array elements & pick the elements from middle and go to extremes as
	 * this order will have min abs diff
	 */
}
